name: event
description: Create lightweight custom Events, that allow interested subscribers to be notified that something has happened.
version: 3.1.0
homepage: https://github.com/aryehof/dart-event

environment:
  sdk: ^3.4.0

#dependencies:
#  path: ^1.6.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0